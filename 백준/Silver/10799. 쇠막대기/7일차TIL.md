### [ 백준 10799번 - 쇠막대기 ]  
---
##  🧾 문제 요약

괄호로 표현된 쇠막대기와 레이저 정보가 주어졌을 때, 자른 쇠막대기 조각의 총 개수를 구하는 문제.  
- `()` → 레이저  
- `(` → 쇠막대기 시작  
- `)` → 쇠막대기 끝

레이저는 자신이 위치한 지점에서 현재 열려 있는 모든 쇠막대기를 자른다.  
쇠막대기 하나가 끝나는 경우에는 조각이 하나 추가된다.

---

## ❌ 실패한 접근 (처음 작성한 코드)

```js
const input = require("fs").readFileSync("/dev/stdin").toString().trim();

let raser = input.replaceAll("()", "R"); // 레이저를 R로 치환

let stick = [];
let result = [];
let stickArr = raser.split("");
stickArr.forEach((st) => {
  if (st === "(") {
    stick.push(1);
  } else if (st === ")") {
    result.push(stick.pop());
  } else if (st === "R") {
    for (let i = 0; i < stick.length; i++) {
      stick[i] += 1;
    }
  }
});
console.log(result.reduce((a, c) => a + c));
```
## 📉 문제점
"()" 를 "R"로 바꾸는 방식은 의미는 명확하지만, 레이저와 쇠막대기의 구조적인 맥락을 잃어버림

중첩 구조에 대해 잘못된 누적 연산 방식 사용

로직이 복잡하고 오류 발생 가능성이 높음 → 백준에서는 런타임 에러 발생

## ✅ 올바른 접근 (스택 기반 풀이)
```js
const input = require("fs").readFileSync("/dev/stdin").toString().trim();

let stack = [];
let result = 0;

for (let i = 0; i < input.length; i++) {
  if (input[i] === "(") {
    stack.push("(");
  } else {
    stack.pop();

    if (input[i - 1] === "(") {
      // 레이저
      result += stack.length;
    } else {
      // 쇠막대기 끝
      result += 1;
    }
  }
}
console.log(result);
```

### 💡 배운 점

- 괄호 문제는 거의 항상 **스택을 사용해서 푸는 게 안정적**이다.
- 레이저는 단순히 `()`로 표현되지만, **레이저의 효과는 열린 괄호의 수만큼 영향을 미침**.
- `()` 형태를 스택에서 직접 판별할 수 있으며, **앞 문자와 현재 문자를 함께 보는 방식**이 유용하다.

---


### 📌 느낀 점

처음에 문자열 치환을 이용해서 문제를 단순화하려고 했지만, 그 접근이 레이저와 쇠막대기의 맥락을 잃게 만들어 오히려 비효율적인 방식이 되었음.

이번 문제를 통해 **정확한 의미 파악과 기본적인 스택 구조의 중요성**을 다시 한번 느꼈다.

다음엔 문제의 의도를 좀 더 신중하게 파악한 뒤 접근해야겠다고 생각했다.
